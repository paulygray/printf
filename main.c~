#include <limits.h>
#include <stdio.h>
#include "main.h"
#define ZERO 0
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
  int n1, n2;
  n1 = _printf("[%d]\n", INT_MIN);
  n2 = printf("[%d]\n", INT_MIN);
  printf("Len:[%d]\n", n1);
  printf("Len1:[%d]\n\n", n2);

  _printf(NULL);
  printf(NULL);
}

